import { useState } from "react";
import "./App.css";
import Header from "./Header.jsx";

function App() {
  const [count, setCount] = useState(0);

  return (
    <div className="app">
      <Header />
      {/* Tinder cards*/}
      {/* Swipe options */}
    </div>
  );
}

export default App;
